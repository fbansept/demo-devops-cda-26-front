name: Docker Image CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: docker build -t fbansept/demo-devops-cda-26-front .

    - name: extract version from package.json
      run: echo "VERSION=$(jq -r '.version' package.json)" >> $GITHUB_ENV

    - name: TAG the Docker image
      run: docker tag fbansept/demo-devops-cda-26-front fbansept/demo-devops-cda-26-front:${{ env.VERSION }}

    - name: Log in to Docker Hub
      uses: docker/login-action@v4
      with:
        username: fbansept
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Push Docker images
      run: |
        docker push fbansept/demo-devops-cda-26-front 
        docker push fbansept/demo-devops-cda-26-front:${{ env.VERSION }}

